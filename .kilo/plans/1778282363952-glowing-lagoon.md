# MOVE(plan-files-to-plans-directory)

## Overview
Move all plan files currently located in the root directory of the project to their appropriate subfolders within the `/plans` directory. This organizes the codebase by categorizing plans based on their type (e.g., FEATURE, BUG, DOCS, etc.).

## Goals
- Organize plan files by type for better project structure
- Ensure no plan files remain in the root directory
- Create any necessary subfolders in `/plans` that do not yet exist

## Current State Analysis
- Root directory contains multiple `.md` files, many following the `TYPE(description).md` naming convention
- `/plans` directory exists with subfolders: `CHORE/`, `DOCS/`, `FEATURE/`, `REFACTOR/`, `STYLE/`
- Some plan files are already in appropriate subfolders within `/plans`
- No conflicts detected (no duplicate filenames in target locations)

## Identified Plan Files in Root
Based on file listing and naming patterns, the following files in the root directory are considered plan files:

### TYPE-based Plans (to move to `/plans/TYPE/`):
- `CHORE(fixpro-ai-rebrand).md` → `/plans/CHORE/`
- `FEATURE(complete-ai-summarization-pipeline).md` → `/plans/FEATURE/`
- `FEATURE(dashboard-ui-redo).md` → `/plans/FEATURE/`
- `FEATURE(estimate-metadata-flow).md` → `/plans/FEATURE/`
- `FEATURE(estimate-summarization).md` → `/plans/FEATURE/`
- `FEATURE(estimate-upload).md` → `/plans/FEATURE/`
- `FEATURE(mastra-ai-pipeline).md` → `/plans/FEATURE/`
- `FEATURE(recent-estimates-widget).md` → `/plans/FEATURE/`
- `FEATURE(session-lifecycle).md` → `/plans/FEATURE/`
- `FIX(infinite-redirect).md` → `/plans/FIX/` (folder needs creation)
- `REFACTOR(modular-architecture).md` → `/plans/REFACTOR/`

### Non-TYPE Plans (to move to `/plans/DOCS/`):
- `ARCHITECTURE.md` → `/plans/DOCS/`
- `CREATE_FEATURE_COMPONENT.MD` → `/plans/DOCS/`
- `CREATE_FEATURE_PAGE.MD` → `/plans/DOCS/`
- `VIOLATIONS.md` → `/plans/DOCS/`

Note: `README.md` and `MUST_FIX_FOR_PRODUCTION.md` are excluded and will remain in root.

## Implementation Steps
1. **Create missing folders**: Create `/plans/FIX/` folder
2. **Move TYPE-based plans**: For each TYPE-based file, move to `/plans/TYPE/filename.md` (preserving original filenames)
3. **Move non-TYPE plans**: Move remaining identified plan files to `/plans/DOCS/` (preserving original filenames)
4. **Verify move**: Confirm all target files exist in correct locations and no identified plan files remain in root (except excluded ones)
5. **Update any references**: Check for any hardcoded paths or references to these files that may need updating

## Risk Assessment
- Low risk: File moves within the same filesystem
- Potential issue: If any files are currently open or referenced by running processes
- Mitigation: Ensure no active processes are using these files during move

## Validation
- Run `ls -la /Users/dev/Projects/fixpro-app-dashboard/*.md` to confirm root is clean (except README.md)
- Run `find /plans -name "*.md" | sort` to verify all files are in `/plans` subfolders

